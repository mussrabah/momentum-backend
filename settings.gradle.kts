pluginManagement {
    repositories {
        gradlePluginPortal() // The default one
        mavenCentral()       // <-- Add this line
        // google() if you have any Android/Firebase dependencies
    }
}

rootProject.name = "momentum-backend"
